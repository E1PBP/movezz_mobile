import 'package:flutter/material.dart';
class FeedsPage extends StatelessWidget {
  const FeedsPage({super.key});

  @override
  Widget build(BuildContext context) {
    // Gunakan Scaffold tapi TANPA AppBar
    return Scaffold(
      body: Center(
        child: Text('Feeds Content Here'),


      ),
    );
  }
}